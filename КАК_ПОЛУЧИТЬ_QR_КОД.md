# 📱 КАК ПОЛУЧИТЬ QR-КОД ДЛЯ EXPO GO

## ПРОБЛЕМА
QR-код не отображается в терминале из-за CI режима.

## ✅ РЕШЕНИЕ

### Вариант 1: Запустить в своем терминале (РЕКОМЕНДУЕТСЯ)

1. Откройте Terminal (приложение на Mac)
2. Перейдите в папку проекта:
```bash
cd /Users/regina/tik-tok/weight-tracker
```

3. Запустите Expo:
```bash
npx expo start
```

4. Через 10-15 секунд появится **большой QR-код** прямо в терминале!

5. Откройте Expo Go на телефоне
6. Отсканируйте QR-код
7. Приложение загрузится!

---

### Вариант 2: Использовать адрес напрямую

Если вы в одной WiFi сети с телефоном:

1. Узнайте IP адрес вашего Mac:
```bash
ipconfig getifaddr en0
```

Или:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

2. Откройте Expo Go на телефоне
3. Нажмите "Enter URL manually"
4. Введите:
```
exp://[ВАШ_IP]:8081
```

Например: `exp://192.168.1.100:8081`

---

### Вариант 3: Использовать туннель (для разных сетей)

Если телефон и компьютер в разных сетях:

```bash
cd /Users/regina/tik-tok/weight-tracker
npx expo start --tunnel
```

Это создаст публичный URL с QR-кодом (работает медленнее).

---

## 🎯 ЧТО ВЫ УВИДИТЕ

После `npx expo start` в терминале появится:

```
› Metro waiting on exp://192.168.1.100:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)

█████████████████████████████████
██ ▄▄▄▄▄ █▀█ █▄▀▄▀ ▀█ ▄▄▄▄▄ ██
██ █   █ █▀▀▀█ ▀ █▀ █ █   █ ██
██ █▄▄▄█ █▀ █▀▀██▄ ▀█ █▄▄▄█ ██
██▄▄▄▄▄▄▄█▄▀ ▀▄█ █▄█ █▄▄▄▄▄▄▄██
...
```

---

## 📱 В EXPO GO

1. Откройте Expo Go
2. Нажмите "Scan QR code"
3. Наведите камеру на QR-код в терминале
4. Приложение загрузится!

---

## 🔧 ЕСЛИ НЕ РАБОТАЕТ

### "Metro server already running"
```bash
lsof -ti:8081 | xargs kill -9
npx expo start
```

### "Unable to resolve module"
```bash
rm -rf node_modules
npm install
npx expo start --clear
```

### QR-код не сканируется
- Убедитесь что телефон и компьютер в одной WiFi
- Или используйте `npx expo start --tunnel`

---

## 💡 СОВЕТ

После первого подключения Expo Go запомнит проект.
Просто откройте Expo Go → Выберите проект из списка "Recently opened"

Не нужно каждый раз сканировать QR-код! 🎉
